@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">datastaf {{ $datastaf->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/datastaf/' . $datastaf->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit datastaf"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/datastaf', $datastaf->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete datastaf',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $datastaf->id }}</td>
                                    </tr>
                                    <tr><th> Nip </th><td> {{ $datastaf->nip }} </td></tr><tr><th> Nama </th><td> {{ $datastaf->nama }} </td></tr><tr><th> Status </th><td> {{ $datastaf->status }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection