@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">dosenwali {{ $dosenwali->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/dosenwali/' . $dosenwali->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit dosenwali"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/dosenwali', $dosenwali->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete dosenwali',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $dosenwali->id }}</td>
                                    </tr>
                                    <tr><th> Dosen Id </th><td> {{ $dosenwali->dosen_id }} </td></tr><tr><th> Mahasiswa Id </th><td> {{ $dosenwali->mahasiswa_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection